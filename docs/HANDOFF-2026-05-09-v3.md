# HANDOFF v3 — 2026-05-09

> **Кому:** следующей нейросети (Devin / Claude / Codex / любому
> coding-agent), которая возьмёт `Itosyro/plan-app` после этой сессии.
> **От кого:** Devin, агент в сессии
> `https://app.devin.ai/sessions/784f17c1135640c0be019c84909305e9`.
> **Цель:** дать тебе всё, чтобы ты возобновил работу за 15 минут
> без вопросов и без перечитывания всей истории git.

Этот документ — **v3**, продолжение `HANDOFF-2026-05-09-v2.md`.
v3 покрывает события после v2: PR #46 (minor fixes) смерджен,
PR #47 (M-6 + M-3) смерджен, PR #48 (docs) смерджен,
PR #49 (M-4 + code review fix) смерджен.
Все 22 findings из REVIEW закрыты.

---

## TL;DR (60 секунд)

- **Репо:** https://github.com/Itosyro/plan-app (Python 3.12, FastAPI
  webhook + aiogram-3, SQLModel + Postgres/SQLite, Alembic, Groq
  llama-3 для AI pipeline). Прод: `https://plan-app-t6nx.onrender.com`,
  бот `@daylirobot`, free-tier Render.
- **main HEAD:** after PR #49 merge.
  204 теста зелёные. mypy 0 errors. ruff чисто. CI зелёный.
- **Закрыто с v2:** PR #46 (M-1/M-2/M-5/M-7/M-8) + PR #47 (M-6 + M-3)
  + PR #48 (docs) + PR #49 (M-4 + code-review fix).
  Все 22 findings из REVIEW-2026-05-09.md полностью закрыты.
- **Открыто:** Phase 5 (Telegram Mini App — не начат).
- **PAT:** в env есть `GITHUB` (token для git push). Для создания PR
  используй `curl POST` с этим же токеном (API `git_pr` не работает
  с текущим PAT).

---

## Что сделано в этой сессии

### PR #46 — Minor cleanup (уже был создан, CI зелёный, смерджен)

Ветка: `devin/1778320409-minor-fixes`. 5 коммитов:

| Коммит | Finding | Описание |
|---|---|---|
| `9135e47` | M-1 + M-2 | Drop unused `pymorphy3`, `razdel`, `asyncpg` from deps |
| `3c885cd` | M-5 | Replace `getattr(logging, ...)` with `_LOG_LEVELS` mapping |
| `055a946` | M-7 | Enrich docstring of `app/api/__init__.py` placeholder |
| `44e2ef5` | M-8 | Speed up `test_e2e_partial_classify_failure_does_not_kill_batch` (2.78s → 1.54s) |
| `e64ad45` | docs | Handoff v2 + PROGRESS.md |

### PR #47 — M-6 + M-3 (создан, CI зелёный, смерджен)

Ветка: `devin/1778322906-m6-m3-cleanup`. 3 коммита:

**M-6: Per-user morning_anchor / evening_anchor**

- `app/db/models.py` — `UserSettings.morning_anchor` (default `"09:00"`) и
  `UserSettings.evening_anchor` (default `"19:00"`), оба `String(5)`.
- `alembic/versions/0006_user_anchor_times.py` — migration с `server_default`.
- `app/ai/time_resolver.py` — `resolve_time()` и `_preprocess()` принимают
  `morning_anchor` / `evening_anchor` kwargs. Перезаписывают статические
  замены «утром» → `в {morning_anchor}`, «вечером» → `в {evening_anchor}`.
- `app/bot/routers/_pipeline.py` — `run_pipeline()` пробрасывает anchors.
- `app/bot/routers/text.py` / `voice.py` — читают anchors из `UserSettings`.
- 2 новых теста: `test_vecherom_custom_anchor`, `test_utrom_custom_anchor`.

**M-3: Split services.py → services/ package**

| Модуль | Ответственность | LOC |
|---|---|---|
| `users.py` | User CRUD + onboarding | ~80 |
| `inbox.py` | InboxEntry + TelegramUpdate idempotency | ~70 |
| `settings.py` | UserSettings queries, mutations, allow-lists | ~130 |
| `tasks.py` | Task/Note/Category/Horizon CRUD + classification + reminders | ~360 |
| `ai.py` | AiRun logging | ~40 |
| `__init__.py` | Re-exports all public names | ~55 |

Zero changes to 19 import sites — `from app.bot.services import ...` works as before.

---

## Карта репо (обновлённая)

```
plan-app/
├── app/
│   ├── ai/                # AI pipeline modules
│   │   ├── router.py      # GroqKeyRouter + call_with_rotation[T]
│   │   ├── splitter.py    # llama-3.1-8b
│   │   ├── classifier.py  # llama-3.3-70b versatile (instructor)
│   │   ├── critic.py      # llama-3.3-70b instant
│   │   ├── courier.py     # template-based + LLM rephrase
│   │   ├── reorder.py     # detects "переместить X на Y" intents
│   │   ├── time_resolver.py  # dateparser + russian preprocessor + M-6 anchors
│   │   └── whisper.py     # whisper-large-v3
│   ├── bot/
│   │   ├── digest.py      # tick_digests (idempotent)
│   │   ├── services/      # ← M-3: was services.py (739 LOC)
│   │   │   ├── __init__.py   # re-exports all public names
│   │   │   ├── users.py      # User CRUD + onboarding
│   │   │   ├── inbox.py      # InboxEntry + TelegramUpdate
│   │   │   ├── settings.py   # UserSettings queries/mutations
│   │   │   ├── tasks.py      # Task/Note/Category/Horizon CRUD
│   │   │   └── ai.py         # AiRun logging
│   │   ├── routers/
│   │   │   ├── _pipeline.py  # get_groq_router / run_pipeline
│   │   │   ├── text.py       # text message handler
│   │   │   ├── voice.py      # voice message handler
│   │   │   ├── commands.py   # /today, /week, /notes, etc.
│   │   │   ├── callbacks.py  # inline-button handlers
│   │   │   ├── settings.py   # /settings panel
│   │   │   └── start.py      # /start + onboarding
│   ├── workers/
│   │   ├── scheduler.py   # tick_reminders + tick_digests
│   │   └── runner.py      # in-process loop
│   ├── db/
│   │   ├── base.py        # async engine + sessionmaker
│   │   └── models.py      # User, UserSettings (+M-6 anchors),
│   │                      # Task, Note, Reminder, Category, etc.
│   ├── shared/
│   │   ├── config.py      # pydantic-settings
│   │   ├── logging.py     # structlog (M-5: _LOG_LEVELS)
│   │   └── time.py        # utcnow_naive, to_naive_utc
│   ├── api/__init__.py    # placeholder for Phase 5
│   └── main.py            # FastAPI factory, lifespan, webhook
├── alembic/versions/      # 6 миграций: 0001..0006
├── tests/                 # 22 файла, 204 теста
├── docs/
│   ├── PLAN.md            # что строим
│   ├── ARCHITECTURE.md    # как устроено
│   ├── ROADMAP.md         # фазы
│   ├── PROGRESS.md        # хронологический лог
│   ├── REVIEW-2026-05-09.md  # 22 findings (3C + 7I + 8M)
│   └── HANDOFF-2026-05-09-v3.md  # ← этот файл
└── .agents/skills/        # 17 skills
```

---

## Статус findings из REVIEW-2026-05-09.md

### Critical (3/3 закрыты — PR #45)
- ✅ C-1: tz-naive UTC discipline
- ✅ C-2: parse_mode="Markdown" removed
- ✅ C-3: digest idempotency guards

### Important (7/7 закрыты — PR #45)
- ✅ I-1: GroqKeyRouter.advance() + call_with_rotation
- ✅ I-2: week_due_semantic allow-list fix
- ✅ I-3: Critic confidence_threshold from UserSettings
- ✅ I-4: Extracted _pipeline.py
- ✅ I-5: Removed dead reminder_extractor.py
- ✅ I-6: Removed parse_mode from settings callbacks
- ✅ I-7: webhook user_id populated

### Minor (8/8 закрыты — PR #46 + PR #47 + PR #49)
- ✅ M-1: Drop unused pymorphy3
- ✅ M-2: Drop unused razdel + asyncpg
- ✅ M-3: Split services.py into package (PR #47)
- ✅ M-4: Drop format_exc_info + fix root cause (PR #49)
- ✅ M-5: Replace getattr(logging, ...) with _LOG_LEVELS
- ✅ M-6: Per-user morning_anchor / evening_anchor (PR #47)
- ✅ M-7: Enrich app/api/__init__.py docstring
- ✅ M-8: Speed up slow e2e test

---

## Что делать дальше (приоритет)

### 1. Phase 5: Telegram Mini App (основная работа)

Не начат. Из `ROADMAP.md`:

- React + Vite + Tailwind (папка `webapp/`)
- Три вкладки: Канбан / Календарь / Список
- FastAPI JSON API в `app/api/` (placeholder уже есть)
- Авторизация через Telegram `initData`
- Drag-n-drop задач между горизонтами
- Карточка задачи → оригинальный транскрипт

### 2. Phase 6: Polish

- LLM evals (golden-set 50 фраз)
- pre-commit hooks
- mypy strict
- backup БД

---

## Команды для возобновления работы

```bash
cd /home/ubuntu/repos/plan-app
git fetch origin && git checkout main && git pull --ff-only origin main

# Проверка среды
uv sync
uv run ruff format --check .   # должно быть чисто
uv run ruff check .             # должно быть чисто
uv run mypy                     # 0 errors
uv run pytest --tb=short -q     # 204 passed
```

---

## Известные подводные камни

1. **Git proxy** может не видеть repo — используй PAT URL для push:
   `git push "https://x-access-token:${GITHUB}@github.com/Itosyro/plan-app.git" <branch>`
2. **`git_pr` API** не работает с текущим PAT — используй `curl POST` для PR.
3. **`instructor.max_retries=2`** — артефакт, НЕ удалять.
5. **Render free-tier** — засыпает через 15 мин, cold start ~30 сек.
6. **DateTime** в БД — всегда tz-naive UTC. Никогда tz-aware.
7. **`parse_mode="Markdown"`** — ЗАПРЕЩЁН для user-controlled текста.

---

## Скиллы (.agents/skills/)

Обязательно активировать перед работой:

**Постоянные:** `plan-app-internal`, `using-uv`, `defensive-programming`,
`code-review`, `writing-prs`.

**По ситуации:** `aiogram-3`, `migrations-safely`, `testing-async-python`,
`groq-tips`, `russian-nlp`, `prompt-engineering`, `systematic-debugging`.

---

## Тест-сьют

| Файл | Тестов | Что покрывает |
|---|---|---|
| test_callbacks.py | 10 | inline-button handlers |
| test_classifier.py | 4 | classifier mock |
| test_commands.py | 11 | /today, /week, /notes, /categories |
| test_courier.py | 13 | courier template + LLM |
| test_critic.py | 9 | critic mock |
| test_digest.py | 18 | morning/evening digest |
| test_e2e_phase4.py | 5 | end-to-end phase 4 |
| test_e2e_pipeline.py | 9 | end-to-end pipeline |
| test_groq_router.py | 11 | key rotation |
| test_persistence.py | 10 | DB persistence |
| test_reminders.py | 13 | reminder scheduling |
| test_reorder.py | 9 | task reorder |
| test_runner.py | 4 | scheduler runner |
| test_scheduler.py | 9 | cron worker |
| test_services.py | 6 | services unit |
| test_settings.py | 21 | /settings panel |
| test_shared_time.py | 10 | time utilities |
| test_smoke.py | 4 | smoke tests |
| test_splitter.py | 5 | splitter mock |
| test_time_resolver.py | 12 | time resolver (+M-6 anchors) |
| test_webhook.py | 6 | webhook security |
| test_whisper.py | 5 | whisper mock |
| **Total** | **204** | |
