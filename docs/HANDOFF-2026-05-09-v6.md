# HANDOFF v6 — 2026-05-09

> Кому: следующей нейросети
> От кого: Devin v6 (session https://app.devin.ai/sessions/9bcd4a8da40a4444899f86789b7e245f)
> Цель: дать всё чтобы возобновить работу за 15 минут

## TL;DR

Этой сессии не делалось правок продуктового кода. Всё содержимое —
**docs + skills bundle expansion** для будущих агентов.

Также **смержены** все 10 PR'ов от прошлой сессии (#50–#59). Текущее
состояние main отражает шесть critical-фиксов + три docs-PR'а +
первый skills bundle.

## Текущее состояние main

* HEAD: после `git pull --ff-only` ≥ `d96d439` (PR #59 merged).
* Тесты: **217 passed** (`uv run pytest -q`).
* Ruff format / check: clean.
* Mypy strict: `Success: no issues found in 41 source files`.
* Прод: https://plan-app-t6nx.onrender.com/healthz → 200 OK.

## Что сделано в v5 → закоммичено и смержено в этой сессии

| PR  | Тема                                              | Статус |
|-----|---------------------------------------------------|--------|
| #52 | C-5 webhook idempotency TOCTOU race               | merged |
| #53 | C-1 settings UI: split with maxsplit=3            | merged |
| #54 | C-4 classifier receives existing user categories  | merged |
| #55 | C-6 voice handler loads default_reminder_offsets  | merged |
| #56 | C-2 time-resolver: «сегодня» не уезжает на +7 дней | merged |
| #57 | C-3+I-7 ON DELETE policies for every FK           | merged |
| #50 | docs: super-review v2 (14 findings)               | merged |
| #51 | docs: HANDOFF v4                                  | merged |
| #58 | docs: HANDOFF v5 + PROGRESS.md                    | merged |
| #59 | skills: anthropic + obra bundles + next-prompt v1 | merged |

После мержа Render автоматически задеплоил миграцию **0007** (FK ON
DELETE policies) на Neon. Я НЕ верифицировал руками что миграция
прошла — тыщу раз `/healthz` возвращает 200 OK, что подтверждает что
приложение поднялось (== миграции не упали).

## Что добавлено в этой сессии (v6)

Один PR, **NEW BRANCH**: `devin/<timestamp>-skills-expansion`, **NEW PR
#60** (создаётся в конце сессии).

### 1. Новый skill bundle: `voltagent/`

Кураторская подборка subagent'ов из самой популярной коллекции
[VoltAgent/awesome-claude-code-subagents](https://github.com/VoltAgent/awesome-claude-code-subagents)
(20K+ stars, MIT). Commit `6f804f0c`.

**26 .md файлов** — выбраны те, что релевантны нашему стеку:

* Language: `python-pro`, `fastapi-developer`, `sql-pro`.
* Quality: `code-reviewer`, `architect-reviewer`, `debugger`,
  `error-detective`, `qa-expert`, `test-automator`,
  `security-auditor`, `performance-engineer`.
* Data/AI: `postgres-pro`, `database-optimizer`, `ai-engineer`,
  `llm-architect`, `prompt-engineer`.
* Core: `backend-developer`, `api-designer`, `frontend-developer`,
  `fullstack-developer`.
* Infra: `docker-expert`, `database-administrator`,
  `security-engineer`, `devops-incident-responder`.
* DX: `refactoring-specialist`, `documentation-engineer`.

**Не взяты:** Rust/Go/Java/Scala-эксперты, Angular/Vue, AWS/Azure
specifics, бизнес-роли. Полный список в `.agents/skills/voltagent/SOURCE.md`.

### 2. Новый custom skill: `tg-bot-api/`

Выжимка по актуальной (Bot API 10.0, 8 мая 2026) Telegram Bot API
для plan-app. Что мы используем, чего НЕ используем, что появилось
в свежих версиях (10.0 / 9.6 / 9.5 / 9.4), наши конкретные гочки
(G-1..G-10 для бота).

Источник для актуальной версии:
* https://core.telegram.org/bots/api-changelog (10.0 — guest mode,
  live photos, polls media).
* aiogram 3.26.0 (поддерживает Bot API 9.5).

### 3. Новый custom skill: `python-best-practices/`

12 секций конкретных правил для Python в этом проекте:
async/await (1), типы (2), Pydantic v2 (3), SQLAlchemy 2.0 async
(4), pytest async (5), ruff/mypy (6), uv (7), structlog (8),
naive UTC (9), безопасность (10), производительность (11), CI (12).

Включает выжимку из community-консенсуса 2025-2026 +
наши собственные грабли.

### 4. Полностью переписан `docs/PROMPT-FOR-NEXT-SESSION.md`

Из 400 строк → 730+ строк. Новые секции:

* **Главные правила (10 пунктов)** — поднято в начало. Ключевое:
  *"Ты делаешь всё сам: коммитишь, пушишь, мержишь, обновляешь
  docs. Не проси юзера это делать."*
* **Архитектура (ASCII диаграмма)** — поток webhook → routers →
  pipeline → services → DB → scheduler → reminder.
* **Расширенный onboarding** (6 шагов вместо 4).
* **Что осталось** — Important findings + Phase 5.
* **Workflow для каждой задачи** — A (фикс) / B (фича) / C
  (миграция) / D (рефакторинг).
* **Рабочие команды** — git workflow, curl PR creation, curl PR
  merge, CI checks.
* **Грабли** — расширены до **18 пунктов** (было 15). Добавлены
  G-16 (Bot API 10.0 Guest Mode), G-17 (uv.lock конфликты),
  G-18 (Render не деплоит feature-branch миграции).
* **Что НЕЛЬЗЯ делать** — 12 запретов (force-push, gh CLI, pip,
  amend, и т.д.).
* **Что ОБЯЗАТЕЛЬНО делать** — 9 правил.
* **Шаблон HANDOFF документа** — чтобы каждая сессия имела
  одинаковую структуру отчёта.
* **Шаблон PR описания** — чтобы PR'ы были человекочитаемыми.
* **Контрольные чек-листы** — перед первым коммитом и перед `stop`.

### 5. Обновлён `.agents/skills/CATALOG.md`

* Добавлены строки для новых custom skills (tg-bot-api,
  python-best-practices).
* Добавлена новая секция «Скиллы из VoltAgent/awesome-claude-code-
  subagents» с 26 файлами в 5 подкатегориях.
* Обновлено суммарное саммари размера: ~2.6 МБ, 35+ SKILL.md.

## Что НЕ сделано (задачи следующей сессии)

* **Important findings I-1..I-6, I-8** в `docs/REVIEW-2026-05-09-v2.md`
  — открыты, не трогали.
* **Minor findings M-1..M-9** — открыты.
* **Phase 5 Mini App** — не начат.
* **Не обновлял `docs/ROADMAP.md`** — статус Phase 5 не менял
  (всё ещё запланирован).
* **Не верифицировал миграцию 0007 руками** на проде. `/healthz`
  → 200, значит upgrade прошёл, но конкретные FK constraints
  не проверял через psql.

## Что НЕ делал (граница ответственности)

* НЕ менял продуктовый код.
* НЕ начинал Important findings — пользователь хотел skills + prompt.
* НЕ устанавливал pre-commit hooks (их в проекте нет — `git status`
  не показывает `.pre-commit-config.yaml`).
* НЕ обновлял `devin_env config` — по сути зависимости не менялись,
  и conftest уже стоит правильно.

## Грабли которые наловил в этой сессии

Новых не было — все известные G-1..G-15 повторно подтвердили.
Дополнительно добавил в промпт три, которых не было раньше:

* **G-16** — Bot API 10.0 (8 мая 2026) Guest Mode: бот может
  получать update'ы из чатов где он не участник. Если ловишь
  странный `update.guest_message` — это оно.
* **G-17** — `uv.lock` конфликты при долгоживущих ветках. Не пытайся
  мерджить .lock руками, делай `git checkout main -- uv.lock`,
  потом `uv lock --upgrade`.
* **G-18** — Render auto-deploy миграции **только** при merge в
  main. Feature branches не запускают миграции.

## Тестирование (что я делал руками)

* `uv run ruff format .` — 75 files left unchanged.
* `uv run ruff check .` — All checks passed.
* `uv run mypy` — Success: no issues found in 41 source files.
* `uv run pytest -q` — 217 passed in 7.83s.
* `curl https://plan-app-t6nx.onrender.com/healthz` →
  `{"status":"ok"}`, latency 230 ms.
* НЕ тестировал бота вручную через Telegram — это E2E ответственность
  пользователя; я не имею TG аккаунта в этом окружении.

## Полезные ссылки на код

Не правил код — только docs и skills, ссылки в самом
`docs/PROMPT-FOR-NEXT-SESSION.md`.

## TL;DR для следующего агента

* Открой `docs/PROMPT-FOR-NEXT-SESSION.md` и прочти.
* Открой `.agents/skills/CATALOG.md` и пройди по списку.
* Решай задачу пользователя. Делай ВСЁ сам — мерж, push,
  обновление docs.
* Перед `stop` — создай `HANDOFF-...-v7.md`.

— Devin v6, 2026-05-09
